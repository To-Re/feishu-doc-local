# 发布包审计记录 · 2026-09-13

> 历史验收记录，描述当时版本；当前用法见[使用文档](../README.md)。

本记录保留 2026-09-13 对两个 Obsidian 插件构建目录及后续同步扩展安装包的历史审计事实，核对 JavaScript、样式、资源、构建元数据和许可证。审计过程只读，没有调用飞书 CLI、认证服务或云端文档接口。实验性 IDE 插件已暂停维护，原安装包的检查范围和摘要仅保留在本机的实验分支中，不包含在此次公开版本。

历史结果：在以下检查范围内，未发现这些 Obsidian 构建产物的隐私、许可证缺失或运行依赖边界问题。版本、数量及摘要只对应当时检查的内容；本次分支范围调整没有把旧审计改写为当前产物的新验收。

## 检查对象与摘要

| 构建产物 | 版本 | 大小 |
| --- | --- | ---: |
| `obsidian-plugin/dist/` | 0.2.0 | 211 个文件，共 13,035,388 字节 |
| `obsidian-sync-plugin/dist/` | 0.1.0 | 211 个文件，共 1,177,599 字节 |

重新构建后的安装包应重新核对内容、大小与摘要，不能沿用本表作为验证结果。

## 隐私与资源

当时的内容和文件名扫描覆盖这两份 Obsidian 构建目录，检查本机用户目录、私人身份、公司内部代码域名、内部飞书租户、真实测试文档标识、私钥、GitHub 凭据、AWS 访问标识、JWT 和自研 CLI 引用等模式。

- 未发现私人绝对路径、私人账号、真实测试文档地址或上述凭据模式。
- 未发现用户 XML、旁置评论 JSON、项目索引、认证配置、恢复草稿或原始测试素材随包分发。
- 未包含 `.git`、`.local` 等仓库或运行缓存目录，也未包含 `node_modules` 或 source map。
- 未发现独立图片文件或可解码的内嵌图片数据地址。字体按实际构建资源单独核对，见下文许可证结果。
- Obsidian 的构建元数据仅含相对模块路径和依赖图，未命中私人路径模式。

审计输出没有记录潜在敏感值。本记录也不包含私人绝对路径或真实测试文档 URL。

## 运行依赖边界

| 形态 | 检查结果 |
| --- | --- |
| Obsidian 本地插件 | 构建的唯一外部模块是 `obsidian`；没有 Node 子进程或 HTTP 服务导入，无需另装 Node 或飞书 CLI。 |
| Obsidian 同步扩展 | 使用 Obsidian 桌面运行时提供的文件与子进程模块，并依赖本地基础插件配合；没有 HTTP 服务监听模块导入。通过明确配置调用官方 CLI，不需要额外启动 Node 网页服务。 |

官方 CLI 和按需使用的 Node 运行时由使用者自行安装和管理，登录状态也由 CLI 管理；程序文件名检查不等于验证可执行文件的发行来源。

## 许可证核对

两份 Obsidian 构建目录都能找到本仓对应的 206 份许可证、通知和清单文件，内容哈希与审计时的仓库版本一致。

当时只读执行：

```sh
node scripts/generate-third-party-notices.mjs --check --verify-bundle
```

以下是原轮共享许可检查的结果，计数来自当时全部构建清单，不能作为当前分支重新构建后的数量：

- 核对了 118 个实际进入构建的包路径及输出字体资源。
- 校验了 200 份许可文件，清单覆盖 187 个运行依赖条目、7 个构建工具条目及 60 个字体文件。
- 项目许可证、第三方通知及字体许可均随包保留；官方 CLI 的许可来源记录不表示 CLI 二进制随本项目分发。

## 公开 Git 对象检查

公开分支另行完成了可达 Git 对象扫描。**检查时**，范围为 3 个可达提交、389 个唯一 blob，未发现本机私人路径、内部租户域名或凭据模式。此前的本地历史缓存已不在该公开分支的可达历史中。

该数量是检查时的快照；后续文档提交或重新准备公开分支会改变提交数和对象数。此结论不表示本机所有其他分支、不可达对象或历史工作目录均已删除或完成审计。

## 范围与限制

本记录是构建产物和公开对象的内容审计，不替代真实 Obsidian 图形界面测试、飞书线上往返验收、依赖漏洞审计或法律意见。模式扫描和逐项归因核对只能说明当时范围内未发现问题，不能证明任意编码、未来新增资源或使用者自行生成的文档都不含敏感信息。

使用过程中产生的源文稿、评论、令牌、同步备份与恢复记录不属于公开安装包，应保留在使用者自己的受控存储中。版本或打包范围变化后，应重新核对实际产物，而不是仅复用本记录。

## Obsidian 同步扩展 0.1.1 补充审计

`0.1.1` 增加可选 Node 绝对路径，支持 macOS 图形应用的 npm 版 CLI 启动；CLI 通过参数数组和 `shell: false` 执行，标准输入立即关闭。取消、超时及卸载终止独立进程组中的后代进程；未确认结果保留私有错误回执，不能据进程退出推断云端写入已撤销。此次类型检查、46 项测试和构建通过，另以真实本地测试进程验证调用边界，没有调用真实 CLI、登录或访问云端。

重新构建并逐文件核对的发布包为：

| 产物 | 大小 | 内容 |
| --- | ---: | --- |
| `feishu-doc-local-obsidian-sync-0.1.1.zip` | 385,806 字节 | 210 个发行文件；顶层插件目录为 `feishu-doc-local-sync/` |
| `obsidian-sync-plugin/dist/` | 1,182,742 字节 | 211 个文件；比 ZIP 多一份仅供构建核验的 `build-meta.json` |

ZIP 的 SHA-256：

```text
44044c8fe65e40d3c7d3eb678245bfd62eb7f2c776a2fd87a9091dc526eb2ca1
```

本次对新 ZIP 的全部文件执行私人目录、内部身份与域名、私钥、GitHub 凭据、AWS 访问标识及 JWT 模式扫描，未发现命中；未夹带用户文稿、旁置评论、`data.json`、项目索引、缓存、source map 或 CLI 可执行文件。206 份许可证、通知与清单和仓库逐字节一致；重新执行许可证检查，118 个实际构建包路径、200 份许可文件及字体资源校验通过。

构建外部导入仅为 `obsidian` 和桌面运行时提供的文件、路径、加密及子进程模块，没有 HTTP 监听模块。安装目录已按发行文件列表更新并逐文件比对，升级没有删除或覆盖名单之外的用户文件。旧 `0.1.0` ZIP 保留，新包摘要已加入交付目录的 `SHA256SUMS`。这些检查不代表真实 Obsidian GUI 或插件云端往返已经验收。
