# 安全说明

本地飞书文档提供四种运行方式，共用文档格式，文件访问和网络边界如下。

| 运行方式 | 文件与网络边界 |
| --- | --- |
| 本地服务版 | Node 服务只监听 `127.0.0.1`，读取明确打开的文件及受限资源；仅通过可信本机配置调用外部 CLI。不是面向公网的多租户服务，不应通过反向代理直接向公网暴露。 |
| 静态浏览器版 | 桌面 Chrome 通过目录选择器授予网页读写目录的能力；不启动 Node 服务、不调用 CLI，也不接入飞书同步。实现不上传正文、评论或资源，不把正文或授权句柄持久化到浏览器数据库。 |
| Obsidian 基础插件 | 使用 vault API 读写库内 XML、相邻评论及引用资源。支持本地编辑、评论和源码；恢复草稿保存在插件本地 `data.json`。没有 CLI 或云端同步接口，渲染器和字体随包提供。 |
| Obsidian 飞书同步扩展 | 依赖基础插件，通过显式设置的 CLI 绝对路径和固定参数调用官方协议；使用应用自带运行时，不监听网页端口。正文、评论同步由用户触发；只处理当前 vault 的 XML 和已关联目标。 |

浏览器版仍需从托管站点加载程序和字体，点击外部链接会访问对应网站；这些访问与文档上传不同。目录授权属于网页来源，只有可信部署才应获得授权，范围宜限于稿件目录。刷新后需要重新选择目录。本地构建通过不代表任意托管站点的代码与此仓库一致。

## 数据边界

- 文章、评论、回复、同步快照和素材保存在本机文件中。这些内容可能包含私人信息，不应随代码提交或附在公开问题中。
- 本地服务版从使用者指定的可信配置文件读取 CLI 程序和固定参数；Obsidian 同步扩展从插件设置读取。调用均不通过 shell，不执行文章中指定的命令。CLI 身份、访问令牌与登录由外部 CLI 管理，不由编辑器收集；静态浏览器版与 Obsidian 基础插件不接受 CLI 配置。
- 本地服务版和 Obsidian 同步扩展只有显式关联、新建、同步预览及同步执行等操作才会调用 CLI；预览和身份核对可能读取飞书。正文先预览再确认发布；成功后完整回读并更新本地，覆盖前的原稿与云端回执保留在快照中。结果不确定、资源下载失败或本地出现新修改时停止覆盖。启动、自动保存和项目切换不会自动发布。
- Obsidian 同步扩展执行前要求基础插件保存并锁定对应文档；无效源码、未发送意见及未应用图源草稿需要先处理。同步预览不是写入授权，应用时再次验证本地与云端版本。关闭或卸载后的进程结果可能不确定，已持久化的 pending 记录不可通过盲目重试清除。
- Obsidian 基础插件恢复记录可能含未保存全文、评论草稿和图源。不要公开 `.obsidian`、插件 `data.json` 或其他恢复/同步记录；备份或同步整个 vault 时应自行确认这些文件的目的地和访问范围。
- XML、SVG、外部链接和资源路径按不可信输入处理。未知块保留原始内容，不代表允许执行其脚本或任意加载远程资源。
- 浏览器版只读取授权目录内的文档与明确引用资源；文本预览不执行 HTML、SVG 或代码，白板图形另经 SVG 清理器。Obsidian 插件使用同一套图形清理与公式渲染规则，界面选择器限定在插件视图中。
- 自动保存和恢复日志不能锁住所有外部编辑器。浏览器的 Web Locks 只协调同源页面，XML 与 JSON 也没有跨文件原子提交。遇到版本冲突或未完成保存时保留输入、原文件与恢复记录，先核对实际结果；不要通过删除恢复标记强行重试。

已有验证包括自动化 DOM、模拟 Obsidian 宿主、临时文件 / 进程测试，以及浏览器中的本地与飞书目录交互核对。真实 Obsidian 窗口已按版本完成部分正文编辑、评论生命周期和云端读取预览；完整插件云端往返仍未验收。见 [2026-09-13 验证记录](docs/archive/validation-2026-09-13-plugins.md)，不能把局部操作或模拟宿主结果扩大为全部功能通过。

## 报告漏洞

请勿在公开 Issue 中提交访问令牌、Cookie、真实文档或可利用的私人数据。

仓库公开后，如其 **Security → Report a vulnerability** 入口已开启，请使用 GitHub 私密漏洞报告。若入口不可用，可先在 Issue 中仅请求维护者提供私密联系渠道，不附漏洞利用细节或敏感材料。本项目目前没有承诺固定响应时限或长期支持版本。

报告中尽量提供受影响版本或 commit、运行方式、操作系统及对应的 Node.js / 浏览器 / Obsidian 版本、最小脱敏复现、预期与实际行为，以及是否涉及文件读写、脚本执行或未授权发送。复现应限于自己的本机与测试文档。

本说明适用于网页与 Obsidian 版本。历史实验不属于当前发布包；CI 通过不能解释为所有安全边界均已覆盖。
