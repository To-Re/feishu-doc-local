# 本地飞书文档（feishu-doc-local）

本地飞书格式文档编辑器。面向贡献者与自动化开发工具的协作约定如下；安装与使用见 README，贡献流程见 CONTRIBUTING.md。

`master` 维护四种交付形态，共用 DocxXML 与旁置 JSON：本地服务版、静态浏览器版、Obsidian 本地基础插件及可选飞书同步扩展。Obsidian 基础插件通过 vault API 编辑和评论，同步扩展依赖基础插件并调用用户配置的官方 CLI。静态浏览器版和 Obsidian 基础插件不接受 CLI 配置，不需要安装 Node 或启动本地网页服务。实现和文档必须区分各形态的能力、安全边界与实测范围。

- 开发与测试优先使用公开样例和临时目录。云端验收只操作明确获准的专用测试文档；不要用业务原稿测试覆盖、迁移或删除。发布、登录和权限变更应单独获得授权，不因执行本地测试而扩大操作范围。
- 保留无关的已有改动，不引入未声明的本机路径或其他仓库的运行时依赖。依赖、构建脚本与配置示例应能由新贡献者独立复现。
- 正文使用官方 DocxXML 可交换子集，评论、版本、操作与 AI 回执保存在相邻 `.review.json`。未知结构保留原始 XML，不静默删改。
- 文档提供编辑、只读和源码三种模式。编辑和只读可选文评论；源码直接编辑 DocxXML，校验通过后自动保存，未通过校验的输入单独保留并阻止切文件和云同步。整篇源码修改保留评论但使旧引用位置待确认。正文和已发送评论自动写入真实本地文件。AI 改文件后浏览器空闲时自动读取，有未保存编辑时显示冲突，不覆盖。
- 文档协议以本仓 docs/protocol.md 为准；本地评论不冒充官方云端评论协议。客户端不自动发布；云端兼容性验收需核对同一稿件的本地视觉、磁盘 XML/资源和飞书文件往返。记录 CLI 版本、实际结果与已知差异，不把浏览器粘贴、模拟接口或单元测试当作云端协议通过。
- 本地服务只监听127.0.0.1；读取明确打开的文档、相邻反馈及目录内安全图片。资源树只列当前稿件与显式引用文件；额外文本预览限这些引用中的受支持 UTF-8 普通文件，最多 2 MB，只读且不执行 HTML/SVG/代码。正文白板的 SVG 缓存须按 path/token/src 精确映射，通过受限文本接口读取后清理活动内容、外部引用与越界样式，保留服务端 viewBox。外部资源不自动加载；服务端文本预览拒绝跨目录访问、链接和特殊文件，图片沿用真实路径受限的栅格预览规则。
- 静态浏览器版通过明确的用户操作获取目录读写授权；后台保存只检查权限，不弹出授权窗口。正文与评论写到授权目录，不上传给托管站点、不持久化到浏览器数据库，不接入本机 CLI 或飞书同步。保留双文件版本校验、恢复日志、资源引用白名单和对象 URL 清理；不得把 Web Locks 宣称为覆盖外部进程的文件锁。
- 测试覆盖真实文件保存、并发/恢复、XML原样保留、评论随编辑迁移与已删除选区；交付运行 npm run typecheck、npm test、npm run build。
- 静态浏览器版修改另运行 npm run build:browser，并验证不依赖本地服务或凭据的静态产物；目录 API、权限恢复与保存需区分模拟句柄测试和实际浏览器验收。
- Obsidian 基础插件复用本地编辑器、文章目录和文件协议，支持编辑、评论、源码与自动保存。未发送评论、无效源码和未应用图源草稿保存在插件本地恢复记录，关闭、切文档、重启和恢复失败时不可静默丢弃。插件 `data.json` 可能含完整草稿，不能作为公开源码或截图材料。
- Obsidian 飞书同步扩展是独立的可选插件，必须收到基础插件的同步 lease：先保存并锁定相应文档视图，完成后回读并解锁；未完成草稿应阻止同步。CLI 可执行路径和固定参数只能来自可信设置；不自动登录、扫描凭据或在启动时调用 CLI。新建及写入需要明确操作；「拉取」「推送」在内部完成准备和版本校验后执行，差异预览是可选入口，两端都有修改时仍需明确确认。推送和新建成功后，完整核对云端正文、资源与当前本地版本，再自动更新本地；覆盖前的原稿和回执存档，评论无法可靠迁移时标为待确认。结果不确定、下载失败或本地出现新修改时保留原稿和恢复记录。卸载清理进程、监听与视图，不把未确认的云端写入当作失败而盲目重发。
- 两个 Obsidian 插件的修改分别运行其类型检查、测试和构建；样式限定在插件视图中，不依赖网页服务或远程字体。Obsidian 模拟宿主测试不等于真实应用内验收，模拟 CLI 测试不等于云端往返通过。
- 默认中文界面。使用贡献者自己的 Git 身份；示例、截图和公开回执不得包含真实私人路径、凭证、账号或未获授权的用户内容。原始证据留在被忽略的受控目录，公开报告保留脱敏的步骤、结果和限制。
- 新增或更新资源、依赖时维护 docs/assets-and-attribution.md、THIRD_PARTY_NOTICES.md 与 licenses/。保留第三方作者归因，项目许可证不重新授权用户文档、下载缓存或第三方字体。
